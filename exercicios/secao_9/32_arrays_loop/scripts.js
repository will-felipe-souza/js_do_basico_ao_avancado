let elementos = ["Terra","Água","Ar","Fogo","Éter"];

for(i=0;i<elementos.length;i++) {
    console.log(elementos[i]);
    if(elementos[i] == "Fogo") {
        console.log(`O ${elementos[i]} é contato`);
    }
}